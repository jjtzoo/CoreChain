{
  "name": "corechain-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "overrides": {
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "next": {
      "postcss": "^8.5.23"
    }
  },
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "dev:mobile": "npm run start --workspace apps/mobile",
    "build": "npm run build --workspace apps/web",
    "lint": "npm run lint --workspace apps/web && npm run lint --workspace apps/mobile",
    "typecheck": "npm run typecheck --workspace apps/web && npm run typecheck --workspace apps/mobile && npm run typecheck --workspace @corechain/domain",
    "test": "npm run test --workspace apps/web && npm run test --workspace @corechain/domain",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
