// The QA/QC page's project choice, kept in a cookie for that page only, so
// it is still chosen next time and the server can list just that project.
export const QAQC_PROJECT_COOKIE = "qaqc_project";
