# Operating-system files
Thumbs.db
Desktop.ini

# Local configuration and secrets
.env
.env.*
!.env.example

# Dependency and build output (for future application work)
node_modules/
dist/
build/
.next/
*.tsbuildinfo

# Preserved local source extraction. The curated demo CSVs remain versioned.
**/data/demo/alberta-dig-2024-0022/raw/

# Test and coverage output
coverage/

# Expo (apps/mobile)
.expo/
# expo-env.d.ts is normally gitignored and auto-regenerated by the Expo CLI,
# but nothing in CI ever runs the CLI before `tsc`, so it's committed here
# instead to keep typecheck deterministic. Do not hand-edit its contents.
*.orig.*
apps/mobile/android/
apps/mobile/ios/
